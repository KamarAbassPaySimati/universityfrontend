import React from 'react';
import CardHeader from '../../../components/CardHeader';

const ManageAgent = () => {
    return (
        <CardHeader>
            <div>ManageAgent</div>
        </CardHeader>
    );
};

export default ManageAgent;
