import React from 'react';
import CardHeader from '../../../components/CardHeader';

const Agent = () => {
    return (
        <CardHeader>
            <div>Agent</div>
        </CardHeader>
    );
};

export default Agent;
